package com.renjunxi.mod.bettercopper;

import net.minecraft.fluid.Fluids;
import net.minecraft.item.*;
/*
public class CopperBukkit implements BucketItem{
    public static final BucketItem COPPER_BUKKIT=new BucketItem(Fluids.EMPTY, new Item.Settings().group(ItemGroup.MISC));
    public static final BucketItem COPPER_WATER_BUKKIT=new BucketItem(Fluids.WATER, new Item.Settings().group(ItemGroup.MISC));

    public Item fixBucketItem(Item bucketIn) {
        if (bucketIn == Items.WATER_BUCKET) {
            return (Item)COPPER_WATER_BUKKIT;
        } else if (bucketIn == Items.MILK_BUCKET) {
            return (Item)COPPER_BUKKIT;
        } else {
            return bucketIn == Items.LAVA_BUCKET ? null : bucketIn;
        }
    }
}
*/